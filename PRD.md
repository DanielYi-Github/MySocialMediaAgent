# 社群貼文 AI Agent PRD v2

## 1. 產品定位
社群貼文 AI Agent 是一個本地端、圖片優先的多平台社群文案產生與審核工具。

產品核心不是「直接替使用者發文」，而是先穩定解決：

1. 從圖片與使用者描述理解內容脈絡
2. 產出符合不同平台語氣的草稿
3. 讓使用者快速審核、修改、確認
4. 在明確確認後，才進入半自動或自動發布流程

目前專案已包含自動發文與瀏覽器自動化能力，但 PRD v2 將其重新分層，避免 MVP 範圍與實作能力混在一起。

---

## 2. 目標使用者
首發使用者為「單一內容創作者／個人品牌經營者」，以旅行、生活紀錄、探店、圖文分享為主要場景。

### 使用者特徵
- 有圖片素材，但需要快速轉成多平台文案
- 需要保留人工審核，不希望 AI 直接不可逆發文
- 同一批素材常需要同步到 Instagram、小紅書、Facebook、Threads
- 沒有完整社群團隊，希望降低文案改寫與平台調整成本

### 非首發使用者
- 多人團隊審核流程
- 品牌內容資產庫管理
- 大量排程與成效分析使用者

---

## 3. 版本邊界

### V1：草稿生成與審核 MVP
V1 只承諾完成「產出與審核」，不承諾自動發布成功。

#### In Scope
- 上傳 1–10 張圖片
- 標記首張為主圖
- 輸入補充描述、地點、心情、主題或受眾方向
- 產出 Instagram、小紅書、Facebook、Threads 四平台草稿
- 每平台可獨立修改、複製、重新生成
- 使用者可逐平台確認草稿
- 草稿階段與發布階段在 UI 上明確分離
- 顯示完整提示詞預覽，方便調 prompt

#### Out of Scope
- 不承諾一鍵自動發文
- 不承諾官方帳號授權流程
- 不承諾瀏覽器自動化一定成功
- 不支援排程發文
- 不支援多人審核
- 不支援素材庫、品牌資產庫、成效分析

### V1.5：半自動發布輔助
V1.5 允許把已確認草稿送往發布流程，但必須保持「高風險功能」標示。

#### In Scope
- 已確認草稿才可進入發布區
- 發布前二次確認
- 每平台顯示最終文案與圖片數
- 無官方 API 或高風險平台提供手動複製流程
- 發布失敗顯示可理解錯誤與下一步

### V2：官方 API 發布
V2 才正式承諾官方 API 發布能力。

#### In Scope
- Facebook Page API 發布
- Instagram Business API 發布
- Threads API 發布
- Token 權限檢查
- 圖片公開 URL 中轉或替代方案
- 發布結果記錄

### V2.5：瀏覽器自動化 / Webwright 實驗模式
瀏覽器自動化是實驗功能，不應被視為核心穩定發文能力。

#### In Scope
- 小紅書瀏覽器自動化
- Facebook / Instagram 個人帳號瀏覽器自動化
- Force Webwright 模式
- Self-healing selector
- 失敗時保留可診斷錯誤

#### 必須標示
- 非官方 API
- 可能違反平台使用規則
- 可能因 UI 改版失效
- 發文前必須由使用者明確確認

---

## 4. 核心使用流程

### V1 草稿流程
1. 使用者上傳圖片
2. 系統顯示圖片預覽，首張標為主圖
3. 使用者輸入補充描述
4. 使用者點擊產生草稿
5. 系統呼叫 Vision-capable LLM
6. 系統產出四平台草稿
7. 使用者逐平台審核、修改、重新生成或複製
8. 使用者逐平台確認草稿

### V1.5+ 發布輔助流程
1. 使用者至少確認一個平台草稿
2. 系統顯示發布區
3. 系統列出平台、最終文案、圖片數、帳號類型、風險提示
4. 使用者二次確認
5. 系統執行 API 發布或瀏覽器自動化
6. 系統回報成功、失敗或需要手動處理

---

## 5. 平台輸出規格

每個平台草稿應逐步從單一 `content` 字串升級為結構化資料：

```json
{
  "title": "平台標題或開頭句",
  "body": "主要文案",
  "hashtags": ["#tag1", "#tag2"],
  "imageAlt": "圖片說明",
  "notes": "生成理由或審核提示"
}
```

### V1 相容要求
目前前端仍可使用 `content` 字串，但生成與解析層應往結構化格式遷移。

### Instagram
- 視覺導向、精簡、有畫面感
- 建議 2–4 句
- Hashtag 5–8 個
- 避免過度堆疊標籤

### 小紅書
- 筆記感、生活化、重點條列
- 需有明確標題感
- Hashtag 8–12 個
- 可使用 emoji，但不能影響可讀性

### Facebook
- 敘事完整、自然、親和
- 可接受較長段落
- Hashtag 2–3 個
- 重視可讀性與完整脈絡

### Threads
- 短句、即時感、自然口吻
- 以 1–2 句為主，避免為了短而失去自然感
- Hashtag 0–2 個
- 優先像一句觀察或當下感受，而不是完整廣告文

---

## 6. 圖片理解策略

### V1 實作現況約束
- UI 支援最多 10 張圖片
- 草稿生成目前以首張主圖作為主要 LLM 圖片輸入
- 其他圖片主要用於預覽與後續發布

### V1 要求
- UI 必須明確標示「首張為主圖」
- Prompt 必須說明以主圖為主要分析依據
- 若使用者上傳多張圖，文案不得暗示已完整理解所有圖片，除非模型實際接收多張圖片

### V2 改進
- 支援多圖送入模型
- 或先對每張圖產生摘要，再合併成整體故事線
- 顯示圖片理解摘要，讓使用者修正錯誤辨識

---

## 7. LLM Provider 策略

產品不綁定單一模型。系統支援：

- OpenAI-compatible Chat Completions
- Anthropic Messages API
- Google Gemini OpenAI-compatible API
- NVIDIA NIM
- Ollama local vision models
- Custom OpenAI-compatible endpoint

### 要求
- 預設模型必須是視覺模型
- 若模型不支援圖片，必須給出可操作錯誤訊息
- Provider 設定應與文件一致，不再宣稱只使用單一 Copilot / GPT-4.5-mini 路線

---

## 8. 發布安全要求

### 發布前保護
- 未確認草稿不可發布
- 發布前必須有二次確認
- 二次確認需顯示平台、帳號類型、圖片數、最終文案
- 瀏覽器自動化需額外提示「非官方 API / 可能失效 / 需自行承擔平台風險」

### 憑證與 Token
- V1 可暫存於 localStorage，但 UI 必須標示風險
- V2 應改為本地後端保存，並評估加密儲存
- 不應在 console 或錯誤訊息中暴露完整 token

### Proxy 安全
後端 proxy 不應允許任意 URL 轉發。

最低要求：
- 限制 HTTP method
- 加入 allowlist domains
- 拒絕 localhost / private network upstream
- 對錯誤訊息做 token redaction

---

## 9. 成功標準

### V1 草稿成功標準
- 對 50 組代表性圖片與描述完成測試
- 每組都能產出四平台非空草稿
- 主平台 Instagram / 小紅書 80% 草稿可在少量修改後使用
- 次平台 Facebook / Threads 60% 草稿可在少量修改後使用
- 少量修改定義：文案修改字數低於 20%，且主要結構不需重寫
- 單次草稿生成目標 15 秒內完成；若使用本地模型，允許顯示較長等待提示

### V1.5 發布輔助成功標準
- 使用者能清楚理解哪些平台是 API 發布、哪些是瀏覽器自動化、哪些需手動
- 發布前不會發生誤觸
- 發布失敗時能知道下一步是補憑證、重新登入、換圖片格式或手動發布

### V2 發布成功標準
- API 發布路徑有可重現測試或手動驗證腳本
- Token 權限不足時能回報具體缺少的設定
- 圖片格式錯誤時能提供修復建議

---

## 10. 主要風險

### 產品風險
- 草稿生成與自動發布混在一起，導致 MVP 驗證失焦
- 使用者可能誤以為所有平台都可穩定自動發文
- 瀏覽器自動化看起來很酷，但維護成本高且容易被平台 UI 改版打斷

### 技術風險
- 多 provider response 格式不一致
- 多圖 UI 與單圖 LLM 輸入不一致
- 後端任意 proxy 有安全風險
- localStorage 保存憑證不適合長期使用
- Lint 與測試未納入可靠 CI 流程

### 平台風險
- Meta / Threads 權限與 token 流程複雜
- Instagram 圖片需要公開 URL 與格式限制
- 小紅書無官方公開發文 API，瀏覽器自動化可能違反平台規則

---

## 11. Implementation Plan

### P0：一致性與安全底座
目標：讓產品範圍、文件、UI、技術邊界一致，避免誤觸發布與高風險 proxy。

1. **統一文件定位**
   - 更新 README，明確區分 V1 草稿、V1.5 半自動發布、V2 API 發布、V2.5 實驗自動化
   - 移除「MVP 直接自動發文」暗示
   - 驗證：README、PRD、ARCH 不再互相矛盾

2. **發布區二次確認**
   - 在 `開始發布` 前加入確認 modal
   - 顯示平台、帳號類型、圖片數、文案預覽、風險提示
   - 驗證：未二次確認不會呼叫任何 publish API

3. **後端 proxy allowlist**
   - 限制 `/api/llm/proxy` 可轉發 domain
   - 拒絕 private network / localhost upstream
   - 加入 token redaction
   - 驗證：任意惡意 URL 被拒絕，現有 LLM 與平台 API 可通過

4. **修正 ESLint 環境設定**
   - 對 `server/**`、`test/**` 設定 Node globals
   - 清掉未用 import、空 catch、React hooks 主要錯誤
   - 驗證：`npm run lint` 通過

5. **加入正式 test script**
   - 在 `package.json` 加入 `test`
   - 收斂現有 node 測試輸出
   - 驗證：`npm test` 可在本機一次跑完

### P1：草稿品質與資料模型
目標：讓 V1 草稿生成更穩、更可評估、更符合平台規格。

1. **結構化草稿 schema**
   - 從單一 `content` 遷移到 `title/body/hashtags/imageAlt/notes`
   - 前端保留舊 content 相容層
   - 驗證：舊 response 與新 response 都可渲染

2. **統一 prompt 規格**
   - 將 ARCH、PROMPTS、實作 prompt 的 hashtag 數、字數、語氣統一
   - 建立單一 prompt source of truth
   - 驗證：四平台輸出規格與 PRD 完全一致

3. **多圖策略修正**
   - V1 明確只分析主圖，UI 與 prompt 都說清楚
   - 或實作多圖摘要合併
   - 驗證：多圖上傳不會產生誤導性文案

4. **平台品質檢查器**
   - 檢查空文案、字數、hashtag 數、emoji 過量、Threads 超長
   - 在 UI 顯示提醒，不阻擋使用者修改
   - 驗證：違規草稿能被標出具體問題

5. **50 組素材評估表**
   - 建立手動測試表或 fixtures
   - 記錄平台、修改量、通過率、失敗原因
   - 驗證：能區分問題來自圖片理解、平台改寫或 UI 審核

### P2：發布能力產品化
目標：把已存在的發布能力從「能跑」整理成「可理解、可診斷、可維護」。

1. **發布能力矩陣**
   - 每平台標示 API / Browser / Manual
   - 顯示所需憑證、圖片限制、風險等級
   - 驗證：使用者發布前知道自己走哪條路徑

2. **憑證健康檢查**
   - Facebook / Instagram / Threads token 測試
   - 回報缺少權限或 ID 錯誤
   - 驗證：錯誤不只顯示 API raw message

3. **圖片預處理**
   - 壓縮長邊至 2048px
   - 轉 JPEG 或提示不支援格式
   - 顯示各平台圖片限制
   - 驗證：Instagram / Threads 發圖失敗率下降

4. **Webwright 實驗模式隔離**
   - 將 Force Webwright 放到進階設定
   - 加入明確風險與 debug log
   - 驗證：一般使用者不會誤用實驗模式

5. **發布結果記錄**
   - 本地記錄平台、時間、結果、錯誤摘要
   - 不保存完整 token
   - 驗證：失敗後可回看原因並重試

---

## 12. 推薦下一步
建議先執行 P0。

最小安全切法：
1. 修文件一致性
2. 加發布二次確認
3. 鎖 `/api/llm/proxy`
4. 修 lint/test 腳本

完成 P0 後，再進入 P1 的 schema 與 prompt 品質優化。P2 的自動發文應等 V1 草稿品質穩定後再正式產品化。
